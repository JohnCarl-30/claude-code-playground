# Tiny Shop

A very small shopping-cart library that the playground lets Claude explore and edit.

- `src/cart.js`: cart math (subtotal, discounts, tax)
- `src/cart.test.js`: tests (run with `npm test`)

There is a known bug: a 10% discount code gives the wrong total.
